@model ToDo


<section>
	<div class="container section-padding">
		<div class="row p-5 border rounded">
			<h2>New Task</h2>

			<div asp-validation-summary="All" class="text-danger"></div>

			<form asp-action="Add" method="post">
				<div class="mb-3">
					<label asp-for="Descripltion" class="form-label">Description</label>
					<input asp-for="Descripltion" class="form-control" placeholder="Write Task Descrilption" />
				</div>

				<div class="mb-3">
					<label asp-for="CategoryId" class="form-label">Category</label>
					<select asp-for="CategoryId" class="form-select" asp-items="@(new SelectList(ViewBag.Categories, "CategoryId", "Name"))">
						<option value="">Choose Category</option>
					</select>
				</div>

				<div class="mb-3">
					<label asp-for="DueDate" class="form-label">Due Date</label>
					<input asp-for="DueDate" class="form-control" type="date" placeholder="Choose Due Date" />
				</div>

				<div class="mb-3">
					<label asp-for="StatusId" class="form-label">Status</label>
					<select asp-for="StatusId" class="form-select" asp-items="@(new SelectList(ViewBag.Statuses, "StatusId", "StatusName"))">
					</select>
				</div>

				<button type="submit" class="btn btn-primary">Add</button>
				<a asp-action="Index" class="btn btn-danger">Cancel</a>
			</form>
		</div>
	</div>
</section>

